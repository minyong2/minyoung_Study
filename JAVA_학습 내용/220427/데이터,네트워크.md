## 데이터를 표현하는 단위
    ***  b(bit) ☞ B(Byte) ☞ KB ☞ MB ☞ GB ☞ TB ☞ PB ☞ EB 

- bit 
```
데이터의 최소 표현 단위
1bit는 '0,1' 2가지만 표현

1bit = 2가지 01
2bit = 4가지 0001
3bit = 8가지 0000 0001
...
nbit = 2^n 
```
```
256bit = 1Byte
1024Byte = 1KB
2^10 = 1KB
2^20 = 1MB
2^30 = 1GB
2^40 = 1TB
2^50 = 1PB
2^60 = 1EB

컴퓨터는 0과1만 인식
```

- Byte
```
8bit == 1Byte == 256(2^8)

영어, 로마자, 숫자 표현하기 위해 Byte가 나옴!
왜 8bit를 8Byte라고 할까?

영어50개 + 숫자10개 + 특수문자66개 + 기타문자(국가코드) = 256
```
```
char x = "a";  //1Byte
영어는 1Byte
한글은 2Byte
```

## 하드디스크 vs 메모리
```
(HDD/SSD)
하드디스크 : 저장 공간(데이터가 영구적으로 남아 있음)
1TB, 100TB

메모리 : 저장 공간(데이터가 날아감)
8GB 16GB 32GB
```
```
하드디스크/메모리 특징
1.데이터가 영구적으로 남아 있냐?
2.저장하는 데이터가 다르다.

메모리 : 길동이는 이클립스와 메모장을 실행했다.
메모리에는 이클립스와 메모장이 저장되어 있다.
```


```
서버는 특정 컴퓨터가 아니라 '누구든지' 서버가 될 수 있다.

고객들이 서비스(DB,web,app)에 접속하려면 서버의 주소를 알아야 함.
주소(IP === 도로명 주소)

일반 컴퓨터는 IP를 1개만 가짐

서비스를 이용하기 위해서 주소와 식별번호를 알아야 한다.
IT에서 주소는 'IP'이고 식별번호는 'PORT(포트번호)'라고 한다.

주소(IP) : 192.168.0.252
식별번호(PORT) : 3306 (MySQL)
=> 192.168.0.252:3306 (DB)
=> 192.168.0.252:8080(Spring)


와이파이(공유기 or 라우터) 
와이파이에서 주는 IP는 내부 아이피(가짜 아이피)라고 한다.
광랜(인터넷 선) -- 라우터 => IP
```


```
공유기(라우터)는 문지기 역할~
공유기(라우터)에서 주는 IP는 가짜!

외부IP = public IP
내부IP = private IP
private IP 특징 : 브로드 캐스팅

private IP 사용이유
1.보안
2.아이피 부족


다른 PC에 접속하기 위해서는 외부IP를 알아야 함
```
```
통신을 제공해주는 업체 -> ISP업체
ex) KT,SKT,LG ...
```

```
IP는 총 4Byte

   IPv4 주소 . . . . . . . . . : 192.168.0.28
   서브넷 마스크 . . . . . . . : 255.255.255.0
   기본 게이트웨이 . . . . . . : 192.168.0.1
```

### subnet mask
- 하나의 private를 공유하는 공간

### 마지막 주제
```
고정 IP(public or private): Static IP
    - 특정 한 사람에게만 주는 IP
    - 공유기가 누구에게 어떤 IP를 주었는지 기억함!
    - 초기화 시키려면 공유기를 껏다 켜야함
    - 회사, 메인 서비스 서버(컴퓨터), 

유동 IP(public or private): DHCP(Dynamic Host Configuration Protocol)
    - 카페, 병원, 식당, 집 ...


and OSI 7계층 (물데네 전세표응)
```

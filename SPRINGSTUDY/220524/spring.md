```
FK는 null이 허용됨.

PK => index
FK
```
```
  success : function(response){
          if(response>0){
            alert('회원가입이 완료되었습니다.');
          }

          이 구문 이해하기
```
```
쿼리를 잘 짜면 비즈니스 로직이 줄어든다

SQL을 잘하면 (데이터를 잘 다루면)
웹,앱 다 잘한다.
```

### 세션과 쿠키
```
== 세션 ==
특별한 정보를 서버에 임시로 저장하는 것
데이터 라이프사이클 : 로그인~로그아웃 => 이 범위를 세션이라고 함

http는 정보를 유지하는 기능X 전송만 가능하여 session을 만들었음

☆★HTTP 통신 특징★☆
1.Connectionless (비 연결지향)
더 이상 연결을 지속적으로 하지 않음
2.Stateless (상태정보유지 안함)
요청을 응답하고 접속을 바로 끊어서 정보를 저장하지 않는다.

ex)카톡 보내고 카톡방 나감

이러한 특징때문에 쿠키과 세션을 이용해 간단한 정보를 서버에 '임시'로 저장한다.
백엔드(java)에 저장
쿠키보다 보안이 높다.


== 쿠키 ==

쿠키는 '웹브라우저'에 임시로 저장
프론트(javascript)에서 저장
세션보다 보안이 낮다.

```